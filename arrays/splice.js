let frutas = ['banana', 'uva', 'morango', 'melancia', 'abacate', 'maracuja']

frutas.splice(3, 1)
// Remove os itens do array de acordo com o index, o primeiro parametro do splice é o index do elemento
// o segundo parametro é quantos elementos devem ser removidos a partir dele.

console.log(frutas)